-- prisma/seed.sql
-- This file is automatically run by PostgreSQL on container startup

-- We'll let Prisma handle the schema creation via migrations
-- This file seeds initial data after the schema is set up by the app startup

-- Note: The actual seeding is done via prisma/seed.ts which runs after migrations
-- This file exists as a placeholder for the docker-entrypoint-initdb.d volume mount
-- The real seeding happens via the app container's startup command

SELECT 1;
